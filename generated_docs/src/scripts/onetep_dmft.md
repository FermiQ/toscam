# `src/scripts/onetep_dmft.f90`

## Overview

*   **Purpose:** Orchestrates DMFT (Dynamical Mean-Field Theory) calculations in conjunction with the ONETEP program.
*   **Role in Project:** Acts as a high-level script to manage the iterative DMFT loop, handle input parameters from `input_onetep_dmft.txt`, and interface between ONETEP (for DFT calculations) and the DMFT solver (for impurity problems).

## Key Components

*   **`PROGRAM dmftonetep`**:
    The main executable program. It controls the overall ONETEP-DMFT workflow.
    1.  Initializes timing and input variables using `init_my_input_variables` from the `dmft_variables` module.
    2.  Enters a DMFT iteration loop (`iter_dmft` from 1 up to `niter_dmft`).
    3.  Inside the loop:
        *   Creates directories for the current iteration (e.g., `dir_onetep_iter<iter_dmft>/before_onetep/`, `dir_onetep_iter<iter_dmft>/after_onetep/`) to store input/output files for each step.
        *   Calls `onetep_normal_mode` to execute the ONETEP calculation. This step typically computes the effective non-interacting Green's function or related quantities based on the current self-energy.
        *   If `just_onetep` is false, it proceeds to the DMFT part.
        *   Calls `onetepdmft` to execute the DMFT impurity solver, which calculates a new self-energy based on the results from ONETEP.
        *   Manages the transfer of files (e.g., `sigma_output*`, `green_output*`) between these steps.
    4.  The loop continues until convergence or the maximum number of iterations is reached.

*   **`MODULE dmft_variables`**:
    This module encapsulates most of the global variables, input parameters, and utility subroutines used by the script.
    *   **`FUNCTION build_mpi_command_line(prefix,np,p,omp,mach,mach_arg,args,exe,outputin,hide_errors,localhost,ampersand) AS CHARACTER(2000)`**: A utility function that constructs the full command line string for executing an MPI program. It calls an external helper script `get_mpi_command_line` to get the base MPI command and then prepends/appends necessary arguments, executable name, output redirection, etc.
    *   **`SUBROUTINE replace_it(string1, string2)`**: A wrapper around the `sed` command to perform in-place string replacement of `string1` with `string2` in all files matching `*.dat`.
    *   **`SUBROUTINE flip_flag(sstring, true_to_false)`**: Modifies boolean flags (e.g., from `.TRUE.` to `.FALSE.` or vice-versa) within `*.dat` files (specifically, the `CASE_ONETEP.dat` file). It searches for `sstring` to identify the line and then performs the replacement.
    *   **`SUBROUTINE my_dmft_exe()`**: Retrieves essential paths for the execution environment by calling external shell scripts: `get_onetep_env` (for `dir_onetep`), `get_mpi_env` (for `dir_onetep_mpi`), and `get_onetep_exec` (for `exec_onetep`).
    *   **`SUBROUTINE check_if_input_file_changed()`**: Uses the `namelistmod` module to check if the main input namelist file (`input_onetep_dmft.txt`) has been modified since the last read.
    *   **`SUBROUTINE init_my_input_variables()`**: Initializes all control variables for the script. It defines a namelist `dmftonetep_variables` and reads values from `./input_onetep_dmft.txt`. It also determines `CASE_ONETEP` by reading the `case_onetep` file (generated by `get_onetep_case` script). It performs initial setup like modifying the ONETEP input file for properties calculations if not starting from an old simulation.
    *   **`SUBROUTINE check_flag_consistency()`**: Validates the consistency of various input flags read from the namelist. For example, it ensures that `dmft_split` and `split_onetep` are enabled if `dmft_splitkdmftall` is true. It also adjusts settings if `compute_dos` is true.
    *   **`SUBROUTINE onetep_normal_mode()`**: Manages the execution of the ONETEP program. This is a complex subroutine that handles:
        *   Running ONETEP either as a single MPI job or split into multiple asynchronous jobs (`split_onetep`).
        *   Using different MPI execution styles (`mpi_onetep_type = 1` for custom script, `mpi_onetep_type = 2` for standard MPI calls).
        *   Preparing machine files (`machines_onetep`, or `splitk<i>` if `dmft_splitkdmftall`).
        *   Constructing the appropriate command line using `build_mpi_command_line`.
        *   Waiting for completion if jobs are run asynchronously.
        *   Finally, calls `dmft_collect_script.out` to gather ONETEP outputs.
    *   **`SUBROUTINE onetepdmft()`**: Manages the execution of the DMFT solver part of the calculation. It determines whether to run the serial version (`onetep_split_serial.out`) or the MPI parallel version (`onetep_split.out`) of the DMFT interface code based on the `nproc` variable. It uses `build_mpi_command_line` for the parallel case.

## Important Variables/Constants

Key parameters are defined in `MODULE dmft_variables` and typically read from the namelist file `input_onetep_dmft.txt`:

*   `INTEGER :: niter_dmft`: Maximum number of DMFT self-consistency iterations.
*   `INTEGER :: nproc`: Number of MPI processors to use for the DMFT solver part (if `nproc_mpi_solver` is 1, this implies a hybrid MPI/OpenMP parallelism for the solver if `openmp_solver` > 1).
*   `INTEGER :: nproc_mpi_solver`: Number of MPI processors specifically for the impurity solver (e.g., if the solver itself is MPI parallel).
*   `INTEGER :: nproc_onetep`: Number of MPI processors to use for running ONETEP.
*   `INTEGER :: openmp_solver`: Number of OpenMP threads to use for the DMFT solver.
*   `INTEGER :: nproc_onetep_openmp_`: Number of OpenMP threads per MPI process for ONETEP execution.
*   `CHARACTER(2000) :: CASE_ONETEP`: The base filename for ONETEP's input (`.dat`) and output files.
*   `CHARACTER(2000) :: dir_onetep`: Path to the main ONETEP directory (obtained via `get_onetep_env`).
*   `CHARACTER(2000) :: exec_onetep`: Full path to the ONETEP executable (obtained via `get_onetep_exec`).
*   `CHARACTER(60) :: mach_onetep`: Name of the MPI machine file to be used for ONETEP runs (default: "machines_onetep").
*   `LOGICAL :: start_from_an_old_sim`: If `.TRUE.`, the script attempts to restart from a previous simulation state (expects `sigma_output*` files to be present).
*   `LOGICAL :: just_onetep`: If `.TRUE.`, the script only performs the ONETEP calculation part and then stops. Useful for initial DFT runs or post-DMFT analysis like DOS calculation.
*   `LOGICAL :: compute_dos`: If `.TRUE.`, the script configures and runs ONETEP once to compute the density of states, typically using a converged self-energy from a previous DMFT run.
*   `LOGICAL :: split_onetep`: If `.TRUE.`, ONETEP jobs are run asynchronously when `nproc_onetep > 1`, potentially improving throughput on some systems.
*   `LOGICAL :: all_local_host`: If `.TRUE.`, forces MPI jobs to run on the local machine, overriding machine files.
*   `INTEGER :: restart_from_iteration_number`: If greater than 0, the DMFT loop will start from this iteration number, implying `start_from_an_old_sim = .TRUE.`.
*   `REAL(KIND=DP) :: ed_frequ_min`, `ed_frequ_max`: Minimum and maximum real frequencies for the ED solver when `compute_dos` is active with an ED-based solver.
*   `INTEGER :: ed_real_frequ_last`: Number of real frequency points for the ED solver in the final DOS calculation.
*   `LOGICAL :: dmft_split`: If `.TRUE.`, the ONETEP-DMFT interface code (`onetep_split.out`) is used, which might distribute frequency calculations across MPI tasks.
*   `LOGICAL :: dmft_splitkdmftall`: If `.TRUE.`, enables a mode where different k-points are distributed across batches of MPI processes for the ONETEP part.
*   `INTEGER :: dmft_splitk_batch`: Number of CPUs in each batch when `dmft_splitkdmftall` is active.
*   `LOGICAL :: nomachinefile`: If `.TRUE.`, the `-machinefile` argument is omitted from MPI commands.

## Usage Examples

This program (e.g., compiled as `onetep_dmft.x`) serves as the primary executable for launching and managing a ONETEP-DMFT coupled calculation.

1.  **Prepare Input Files:**
    *   **`input_onetep_dmft.txt`**: A namelist file containing the parameters listed above (e.g., `niter_dmft`, `nproc_onetep`, `CASE_ONETEP`).
    *   **`CASE_ONETEP.dat`**: The main input file for the ONETEP program (e.g., if `CASE_ONETEP = "my_si"`, then `my_si.dat`). This file should be configured for a properties calculation that can interface with DMFT (e.g., compute projected local Green's functions or density matrices).
    *   Optionally, MPI machine files (e.g., `machines_onetep`, `machines_dmft`) if not running on a single node or if `nomachinefile` is false.

2.  **Execute:**
    Run the compiled program from the command line in the directory containing the input files:
    ```bash
    ./onetep_dmft.x
    ```
    The script will then read `input_onetep_dmft.txt`, determine `CASE_ONETEP`, and proceed with the iterative DMFT loop, calling ONETEP and the DMFT solver components as configured.

## Dependencies and Interactions

*   **Internal Dependencies (Fortran Modules):**
    *   `common_def`: Provides utility subroutines, especially `utils_system_call` for executing shell commands.
    *   `genvar`: Supplies global constants, notably `DP` for double precision kind.
    *   `namelistmod`: Used for reading and managing parameters from the `input_onetep_dmft.txt` namelist file.
    *   `strings`: Provides a `string` derived type and associated operations like `replace_in_string`.
    *   `StringManip`: Contains string manipulation utilities like `toString` for converting numbers to strings.
    *   `init_and_close_my_sim`: Though not directly used in the main program flow shown, its presence in `dmft_variables` suggests it might be part of a larger framework for simulation initialization/finalization.
    *   `openmpmod`: Used for OpenMP environment control, like `init_openmp` and `omp_num_threads`.
    *   `timer_mod`: Provides routines for timing different sections of the execution (`initialize_timing`, `finalize_timing`, `start_timer`, `stop_timer`).

*   **External Libraries and Programs:**
    *   **System Utilities:** Heavily relies on standard Unix/Linux command-line utilities such as `mv`, `cp`, `mkdir`, `rm`, `ls`, `grep`, `sed`, `cat`, `sleep`, `wc` for file system operations and text manipulation.
    *   **MPI (Message Passing Interface):** Essential for parallel execution. The script constructs and executes MPI commands (e.g., `mpirun`, `mpiexec`) to run ONETEP and potentially the DMFT solver components across multiple processors or nodes.
    *   **ONETEP:** The ONETEP linear-scaling DFT program is a primary external dependency. This script is specifically designed to drive ONETEP calculations.
    *   **Helper Scripts/Executables:** Assumes the presence of several external scripts and compiled programs, likely located in a `utils` subdirectory relative to `dir_onetep` or in the system PATH:
        *   `get_mpi_command_line`: To generate MPI execution commands.
        *   `get_onetep_env`, `get_mpi_env`, `get_onetep_exec`: To find necessary paths.
        *   `get_onetep_case`: To determine the `CASE_ONETEP` name.
        *   `disable_write` (from `dir_onetep/utils/`): To modify ONETEP input for DMFT.
        *   `onetep.dmft.collect.edc`: To collect data from ONETEP.
        *   `dmft_group_dimers.out`: Post-processing for dimer systems.
        *   `order_list_of_greens.out`: To process and order Green's function files.
        *   `fill_machine_file`, `duplicate_machine_file`: Utilities for creating/modifying MPI machine files.
        *   `dmft_all_iterations.onetep.out`: An MPI wrapper for certain ONETEP execution modes.
        *   `onetep_split.out`, `onetep_split_serial.out`: The DMFT solver interface executables.

*   **Interactions with other components:**
    *   **DMFT Solver:** The script coordinates calls to DMFT solver components. The `onetepdmft` subroutine executes `onetep_split.out` (MPI parallel) or `onetep_split_serial.out` (serial), which are the bridge to the actual impurity solver code (e.g., routines from `src/ed_solver/` or a similar module).
    *   **File System Management:**
        *   Reads its main configuration from `input_onetep_dmft.txt`.
        *   Reads the ONETEP case name from a file named `case_onetep` (which is expected to be generated by `get_onetep_case`).
        *   Interacts with the ONETEP input file `CASE_ONETEP.dat`, potentially modifying it using `sed` (via `replace_it`, `flip_flag`) to control ONETEP's behavior within the DMFT loop (e.g., switching task from `SINGLEPOINT` to `PROPERTIES`, enabling/disabling writing of NGWFs).
        *   Creates and manages a series of iteration-specific directories (`dir_onetep_iter<N>`) to store intermediate files like `sigma_output*` (self-energy from DMFT), `green_output*` (Green's function data), and `edc_output*`.
        *   Uses MPI machine files (e.g., `machines_onetep`, `machines_dmft`) to specify compute nodes for parallel runs.
    *   **Operating System:** Makes numerous `system_call` invocations to run external scripts and commands for file manipulation, job launching, and environment setup.

---
_This document is autogenerated. Manual edits may be required for accuracy and completeness._
